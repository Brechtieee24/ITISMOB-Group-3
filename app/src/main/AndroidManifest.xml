<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permission to read images from device storage for Profile Pic Change -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />

    <!-- GEOLOCATION PERMISSIONS -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LOVELink">

        <receiver android:name=".GeofenceBroadcastReceiver" android:exported="false"/>

        <activity android:name=".HomeActivity" />

        <activity android:name=".MainActivity"
            android:exported="true"
            tools:ignore="Instantiatable">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ViewMembersActivity" />

        <activity
            android:name=".ResidencyHistoryActivity"
            android:exported="false" />
        <activity
            android:name=".ActivityHistoryActivity"
            android:exported="false" />
        <activity
            android:name=".LogResidencyTimeInActivity"
            android:exported="false" />
        <activity
            android:name=".LogResidencyTimeOutActivity"
            android:exported="false" />
        <activity
            android:name=".ProfileActivity"
            android:exported="false" />
        <activity
            android:name=".ViewOtherProfileActivity"
            android:exported="false" />

        <activity android:name=".MembersPageActivity"
            android:exported="false"/>

    </application>

</manifest>
